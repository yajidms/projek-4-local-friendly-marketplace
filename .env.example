# Backend URL that the Flutter app will call.
# Use an HTTP API in front of MongoDB; Flutter cannot connect to MongoDB directly.
AUTH_API_URL=https://api.example.com
# Optional aliases if you prefer a different name.
API_BASE_URL=https://api.example.com
MONGODB_URL=https://api.example.com